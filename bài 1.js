
arr1=[]
function squareNumber(){
   const a= document.getElementsByClassName('a')[0]
   const b=document.getElementsByClassName('b')[0]
   for (arr[i]=0;arr[i]++;arr1[i]>arr.length){
    if(a[i]^2<arr[i]<b[i]^2,arr1[i]>arr.length){
      arr1.push(arr[i])
    }
   }
   const btn=document.querySelector('button')
   document.write(`Các số chính phương trong khoảng từ ${a[i][0]} đến ${b[i][0]}:`)=btn
   

}

